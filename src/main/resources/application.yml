#spring.application.name=springboottesting1
spring:
  application:
    name: springboottesting1
  datasource:
    url: jdbc:postgresql://localhost:5432/SpringBootTestingDatabase
    username: postgres
    password: saimun
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080
